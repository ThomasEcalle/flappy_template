import 'package:flutter/material.dart';

class TemplateHome extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Hello from Flappy Template"),
      ),
    );
  }
}
